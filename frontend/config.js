// URL base da API backend.
// - Desenvolvimento local: deixe como está (usa http://127.0.0.1:5000).
// - Produção (Render): substitua a URL abaixo pela URL do seu Web Service no Render,
//   por exemplo: 'https://forense-backend.onrender.com'
// - Alternativamente, defina window.API_URL antes de carregar este arquivo.
const API = window.API_URL || 'http://127.0.0.1:5000';
